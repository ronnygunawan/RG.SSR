// SolidJS placeholder - in a real deployment, use the actual SolidJS library from npm or CDN
(function (global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports) :
    typeof define === 'function' && define.amd ? define(['exports'], factory) :
    (global = typeof globalThis !== 'undefined' ? globalThis : global || self, factory(global.solidjs = {}));
})(this, (function (exports) {
    'use strict';
    
    const createSignal = (initialValue) => {
        let value = initialValue;
        const read = () => value;
        const write = (newValue) => {
            value = typeof newValue === 'function' ? newValue(value) : newValue;
        };
        return [read, write];
    };
    
    const createEffect = (fn) => fn();
    const createMemo = (fn) => fn;
    const createResource = (fetcher) => [() => ({ loading: false }), { mutate: () => {}, refetch: () => {} }];
    const onMount = (fn) => fn();
    const onCleanup = () => {};
    const createContext = () => {};
    const useContext = () => {};
    
    exports.createSignal = createSignal;
    exports.createEffect = createEffect;
    exports.createMemo = createMemo;
    exports.createResource = createResource;
    exports.onMount = onMount;
    exports.onCleanup = onCleanup;
    exports.createContext = createContext;
    exports.useContext = useContext;
}));
